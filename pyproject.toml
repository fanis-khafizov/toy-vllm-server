[project]
name = "vllm-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.124.2",
    "vllm>=0.12.0",
    "httpx>=0.27.2",
    "uvicorn>=0.32.1",
]
